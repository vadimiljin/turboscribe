# Инструкция по запуску стандартизации Product Review

## 🚀 Быстрый старт

### Тестовый режим (один файл)

**1. Дефолтный тестовый файл** (4.Product_Review_29_May.md):
```bash
cd /home/vadim/Projects/route4me.com/turboscribe
python standardize_product_reviews.py --test
```

**2. Конкретный файл:**
```bash
python standardize_product_reviews.py --test --file="3. Product Review 22 May/3.Product_Review_22_May.md"
```

**3. В фоновом режиме с логом:**
```bash
python standardize_product_reviews.py --test > test.log 2>&1 &

# Следить за прогрессом:
tail -f test.log

# Проверить статус:
ps aux | grep "python.*standardize" | grep -v grep
```

### Обработка всех файлов

```bash
python standardize_product_reviews.py
```

## 📊 Результаты

### Выходные файлы:
- **`*_CLEAN.md`** - стандартизированный файл
- **`employees.md`** - база сотрудников
- **`unattributed_names.md`** - имена без email (с путем и номером строки!)
- **`standardization_log.jsonl`** - лог обработки

### Проверка результата:
```bash
# Посмотреть начало файла
head -100 "Product_Review_2025/4. Product Review 29 May/4.Product_Review_29_May_CLEAN.md"

# Количество строк
wc -l "Product_Review_2025/4. Product Review 29 May/4.Product_Review_29_May_CLEAN.md"

# Поиск конкретного имени
grep -n "Владимир" "Product_Review_2025/4. Product Review 29 May/4.Product_Review_29_May_CLEAN.md" | head -20
```

## ✅ Что сделано

1. ✅ **`emails.csv`** - единственный источник правды
2. ✅ **481 вариант имен** - автоматическая генерация (полное имя, первое, последнее, уменьшительные, все падежи)
3. ✅ **LLM disambiguation** - разрешение конфликтов имен (Владимир → Vladimir Fedorov)
4. ✅ **"Вова" = уникальный** - всегда `vova@route4me.com`
5. ✅ **Все уменьшительные** - Дмитрий→Дима, Юрий→Юра, Виктор→Витя, Вадим→Вадик, и т.д.
6. ✅ **`unattributed_names.md`** - с путем к файлу и номером строки

## 🔍 Примеры разрешений

При обработке `4.Product_Review_29_May.md`:

```
[DISAMB] Разрешение для 'Александр' (3 кандидатов, 37 упоминаний)...
[OK] 'Александр' → Alexandr Kovtunov (alexkovtunov@route4me.com)

[DISAMB] Разрешение для 'Алексей' (2 кандидатов, 6 упоминаний)...
[OK] 'Алексей' → Alexey Afanasiev (alexey@route4me.com)

[DISAMB] Разрешение для 'Владимир' (2 кандидатов, 8 упоминаний)...
[OK] 'Владимир' → Vladimir Fedorov (vladimirfedorov@route4me.com)

[DISAMB] Разрешение для 'Игорь' (2 кандидатов, 42 упоминания)...
[OK] 'Игорь' → Igor Golovtsov (igorgolovtsov@route4me.com)
```

## 📈 Статистика обработки

**Файл:** `4.Product_Review_29_May.md` (95.4 KB)

- **Стратегия:** Chunked mode (≥50KB)
- **Chunks:** 26 (1 заголовок + 25 тем)
- **Параллелизм:** 5 потоков
- **Найдено email:** 8 уникальных
- **Найдено имен без email:** 22
- **Разрешено конфликтов:** 8
- **Время:** ~2-3 минуты
- **Результат:** 107 KB

## 🐛 Troubleshooting

**Проблема:** Скрипт долго работает  
**Решение:** Это нормально! LLM обрабатывает каждую тему + разрешает конфликты имен.

**Проблема:** `OPENAI_API_KEY не найден`  
**Решение:** 
```bash
# Проверьте .env файл
cat .env | grep OPENAI_API_KEY

# Или установите вручную:
export OPENAI_API_KEY='your-key-here'
```

**Проблема:** Имя не разрешено правильно  
**Решение:** 
1. Проверьте `emails.csv` - есть ли там этот человек
2. Посмотрите лог - как LLM принял решение
3. Проверьте `unattributed_names.md` - возможно, имя там

## 📁 Структура файлов

```
turboscribe/
├── standardize_product_reviews.py  # Главный скрипт
├── emails.csv                       # База сотрудников (ИСТОЧНИК ПРАВДЫ!)
├── unattributed_names.md            # Неизвестные имена
├── employees.md                     # Автогенерируемая база
├── .env                             # OPENAI_API_KEY
└── Product_Review_2025/
    ├── STANDARDIZATION_RULES.md     # Правила
    ├── standardization_log.jsonl    # Лог
    └── 4. Product Review 29 May/
        ├── 4.Product_Review_29_May.md         # Оригинал
        └── 4.Product_Review_29_May_CLEAN.md   # Результат
```
