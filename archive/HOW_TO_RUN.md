# Как запускать скрипт

## 🎯 Два режима работы

### 1️⃣ ТЕСТОВЫЙ РЕЖИМ (один файл)

```bash
python standardize_product_reviews.py --test
```

**Что происходит:**
- ✅ Обрабатывает **ОДИН** файл для проверки
- ✅ Дефолтный файл: `4. Product Review 29 May/4.Product_Review_29_May.md`
- ✅ Вывод в консоль (или перенаправьте: `> test.log`)
- ✅ Быстро (~2 минуты с 15 потоками)

**Конкретный файл:**
```bash
python standardize_product_reviews.py --test --file="3. Product Review 22 May/3.Product_Review_22_May.md"
```

**Результат:**
- Файл: `4.Product_Review_29_May_CLEAN.md` (в той же папке)
- Лог: вывод в консоль или перенаправленный файл

---

### 2️⃣ ОБЫЧНЫЙ РЕЖИМ (все файлы)

```bash
python standardize_product_reviews.py
```

**Что происходит:**

1. **Сканирование:**
   - Ищет ВСЕ файлы `*.md` в `Product_Review_2025/`
   - Показывает список файлов (NEW или СУЩЕСТВУЕТ)
   - **Пропускает** файлы с уже существующим `*_CLEAN.md` (если `skip_existing=True`)

2. **Подтверждение:**
   ```
   📋 Найдено файлов для обработки: 15
   
   1. [🆕 NEW] 1. Product Review 15 Jan  | 1.Product_Review_15_Jan.md
   2. [🆕 NEW] 2. Product Review 22 Jan  | 2.Product_Review_22_Jan.md
   ...
   
   Нажмите Enter для начала обработки...
   ```
   **❗ Скрипт ждет вашего Enter!**

3. **Обработка:**
   - Обрабатывает файлы по очереди
   - С **15 потоками** на каждый файл (~2 мин/файл)
   - Показывает прогресс в консоли

4. **Результат:**
   - Каждый файл → `*_CLEAN.md` в той же папке
   - **Лог:** `/home/vadim/Projects/route4me.com/turboscribe/Product_Review_2025/standardization_log.jsonl`

---

## 📝 ГДЕ ЛОГ?

### Тестовый режим (`--test`):
```bash
# Вывод в консоль (если не перенаправили)
python standardize_product_reviews.py --test

# ИЛИ в файл (если перенаправили):
python standardize_product_reviews.py --test > my_test.log 2>&1
```

### Обычный режим (все файлы):
```
📍 JSONL лог:
/home/vadim/Projects/route4me.com/turboscribe/Product_Review_2025/standardization_log.jsonl
```

**Формат лога:**
```json
{"timestamp": "2025-11-15T03:20:00", "file": "...", "status": "success_chunked", "details": "..."}
{"timestamp": "2025-11-15T03:22:00", "file": "...", "status": "error", "details": "..."}
```

**Просмотр:**
```bash
# Последние 10 записей
tail -10 Product_Review_2025/standardization_log.jsonl

# Красиво отформатировать
tail -1 Product_Review_2025/standardization_log.jsonl | python -m json.tool

# Только ошибки
grep '"status": "error"' Product_Review_2025/standardization_log.jsonl

# Только успехи
grep '"status": "success' Product_Review_2025/standardization_log.jsonl
```

---

## 📊 Структура результатов

```
Product_Review_2025/
├── standardization_log.jsonl          # ← ЛОГ ВСЕХ ФАЙЛОВ
├── 1. Product Review 15 Jan/
│   ├── 1.Product_Review_15_Jan.md     # оригинал
│   └── 1.Product_Review_15_Jan_CLEAN.md  # ← результат
├── 2. Product Review 22 Jan/
│   ├── 2.Product_Review_22_Jan.md
│   └── 2.Product_Review_22_Jan_CLEAN.md  # ← результат
└── ...
```

---

## 🚀 Примеры использования

### 1. Обработать один файл для теста:
```bash
cd /home/vadim/Projects/route4me.com/turboscribe
python standardize_product_reviews.py --test > test_result.log 2>&1

# Проверить результат
tail -50 test_result.log
```

### 2. Обработать все файлы:
```bash
cd /home/vadim/Projects/route4me.com/turboscribe
python standardize_product_reviews.py

# Скрипт покажет список и спросит подтверждение
# Нажмите Enter для начала
```

### 3. Обработать все файлы в фоне:
```bash
cd /home/vadim/Projects/route4me.com/turboscribe
nohup python standardize_product_reviews.py > batch_processing.log 2>&1 &

# Следить за прогрессом
tail -f batch_processing.log

# После нажатия Enter в интерактивном режиме
```

### 4. Принудительно переобработать ВСЕ (даже с _CLEAN):
Нужно изменить в коде:
```python
standardizer.process_all_files(skip_existing=False, max_files=None)
```

---

## ⚙️ Настройки в коде

**В методе `process_all_files`:**

```python
# Пропускать файлы с существующим *_CLEAN.md
skip_existing=True

# Ограничить количество файлов (None = все)
max_files=None  # или max_files=5 для теста
```

---

## 📈 Производительность

**С 15 потоками (Tier 3):**
- 1 файл (95KB, 25 тем): **~2 минуты**
- 10 файлов: **~20 минут**
- 50 файлов: **~100 минут** (~1.5 часа)

**Стоимость (gpt-4o-mini):**
- ~$0.03 за файл (95KB)
- 100 файлов = **~$3**

---

## ❗ Важно

1. **Интерактивный режим:** В обычном режиме скрипт **ждет Enter** - не забудьте нажать!
2. **Лог в JSONL:** Для просмотра используйте `tail`, `grep` или `python -m json.tool`
3. **Skip existing:** По умолчанию пропускает файлы с `*_CLEAN.md`
4. **Безопасность:** 15 потоков безопасно для Tier 3 (5,000 RPM)

