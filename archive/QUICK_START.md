# Быстрый старт - Скачивание Zoom записей

## 🚀 Три шага до запуска

### 1. Установка
```bash
pip install playwright
playwright install chromium
```

### 2. Тестовый запуск (РЕКОМЕНДУЕТСЯ)
```bash
# Скачать только первую запись для проверки
python download_recordings.py --test

# Или скачать конкретную запись по индексу (0-based)
python download_recordings.py --test --index 5
```

### 3. Полный запуск (после проверки)
```bash
python download_recordings.py
```

---

## 📋 Что будет происходить

Для каждой записи скрипт:
1. ✅ Откроет Zoom URL
2. ✅ Введет пароль
3. ✅ Нажмет "Watch Recording"
4. ✅ Найдет ссылку "Download (3 files)"
5. ✅ Скачает три файла: .vtt, .mp4 (видео), .mp4 (аудио)
6. ✅ Переименует файлы на основе директории

**Результат:** В каждой директории будут 3 файла с правильными именами:
- `[slug]-transcript.vtt` - субтитры
- `[slug]-video.mp4` - полное видео
- `[slug]-audio.mp4` - только аудио

Пример для `40. Product Review 23 Oct`:
- `40-product-review-23-oct-transcript.vtt`
- `40-product-review-23-oct-video.mp4`
- `40-product-review-23-oct-audio.mp4`

---

## 🔍 Проверка в тестовом режиме

Скрипт выведет подробную информацию:
- **ДИРЕКТОРИЯ СОХРАНЕНИЯ** - полный путь куда сохраняются файлы
- **SLUG ДЛЯ ФАЙЛОВ** - какой slug используется
- **ОЖИДАЕМЫЕ ИМЕНА ФАЙЛОВ** - какие имена получат файлы
- **Тип файла** - субтитры/видео/аудио
- **ПОЛНЫЙ ПУТЬ** - абсолютный путь каждого файла
- **Размер файла** - размер в байтах после скачивания

---

## ⚙️ Настройки (опционально)

Откройте `download_recordings.py` и измените:

```python
# Количество параллельных загрузок (по умолчанию 2)
max_concurrent=2  # можно увеличить до 3-5

# Показывать браузер (False) или скрыть (True)
headless=False  # для отладки оставьте False
```

Или используйте параметры командной строки:
```bash
python download_recordings.py --concurrent 3  # 3 параллельных загрузки
python download_recordings.py --test --index 10  # Протестировать 10-ую запись
```

---

## 🐛 Если что-то пошло не так

1. **Проверьте логи:** `download_recordings.log`
2. **Запустите с видимым браузером:** `headless=False` (уже установлено)
3. **Проверьте одну запись вручную** - откройте URL, введите пароль, посмотрите структуру страницы

---

## 📚 Подробная документация

См. `README_DOWNLOAD.md` для детальной информации.

