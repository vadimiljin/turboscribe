# Как используются данные из zoom.vtt

## 📋 Что извлекается из zoom.vtt:

```python
# Для каждого сегмента в zoom.vtt:
{
    "start": 189.04,        # ✅ ИСПОЛЬЗУЕТСЯ
    "end": 189.94,          # ✅ ИСПОЛЬЗУЕТСЯ
    "speaker": "Dan Khasis", # ✅ ИСПОЛЬЗУЕТСЯ
    "text": "Да слушаю"     # ❌ НЕ ИСПОЛЬЗУЕТСЯ (ломаный!)
}
```

## 🔄 Алгоритм выравнивания:

### Шаг 1: Парсинг входных данных

```python
# zoom.vtt → 1430 сегментов
zoom_segments = [
    {start: 189.04, end: 189.94, speaker: "Dan Khasis"},
    {start: 189.94, end: 195.88, speaker: "Serhii Kasainov"},
    ...
]

# turboscribe.vtt → 320 сегментов
turbo_segments = [
    {start: 189.04, end: 195.00, text: "Так, от меня маленький апдейт..."},
    {start: 195.00, end: 200.00, text: "Мы очень долго к этому шли..."},
    ...
]
```

### Шаг 2: Для каждого turboscribe сегмента найти спикера

```python
for turbo_seg in turbo_segments:
    # 1. Найти ВСЕ zoom сегменты с time overlap
    candidates = []
    
    for zoom_seg in zoom_segments:
        overlap = calculate_time_overlap(zoom_seg, turbo_seg)
        if overlap > 0:
            candidates.append({
                'speaker': zoom_seg.speaker,  # ← Имя из zoom.vtt
                'overlap': overlap
            })
    
    # 2. Выбрать спикера с максимальным overlap
    if one_speaker_dominates (>80%):
        speaker = top_candidate.speaker
    else:
        # 3. Спросить LLM для спорных случаев
        speaker = ask_llm(turbo_seg.text, candidates)
    
    # 4. Результат
    result = {
        'speaker': speaker,        # ← Из zoom.vtt
        'text': turbo_seg.text,    # ← Из turboscribe.vtt
        'start': turbo_seg.start,
        'end': turbo_seg.end
    }
```

## 🤖 Как работает LLM (для спорных случаев):

### Пример спорного случая:

```
Turboscribe сегмент [189.04 - 195.00]:
Текст: "Так, от меня маленький апдейт по снапшотам"

Кандидаты из zoom.vtt:
1. Dan Khasis: 2 секунды (33% времени)
2. Serhii Kasainov: 4 секунды (67% времени)

LLM не может определить по времени - нужен контекст!
```

### Что отправляется в LLM:

```
Предыдущие фрагменты (для контекста):
- **Igor**: "Давайте подождем чтобы Дэн сказал"
- **Dan**: "Да, я слушаю"

ТЕКУЩИЙ ФРАГМЕНТ [189.04 - 195.00]:
"Так, от меня маленький апдейт по снапшотам"

Кандидаты (по времени overlap):
1. Dan Khasis: 33% времени
2. Serhii Kasainov: 67% времени

Вопрос LLM: Кто говорит текущий фрагмент?
```

### Ответ LLM:

```json
{
  "speaker": "Serhii Kasainov",
  "confidence": 0.95,
  "reasoning": "Фраза 'от меня маленький апдейт' указывает 
                на начало выступления, а Serhii занимает 67% 
                времени, что подтверждает его как основного 
                спикера."
}
```

## 📊 Статистика использования zoom.vtt:

```
95.6% сегментов - ТОЛЬКО time overlap (БЕЗ LLM)
  ↓
  Один спикер из zoom.vtt занимает >80% времени
  → Прямое назначение

3.1% сегментов - time overlap + LLM
  ↓
  Несколько спикеров из zoom.vtt с близким overlap
  → LLM анализирует текст + контекст + имена кандидатов

1.2% сегментов - fallback
  ↓
  Нет overlap с zoom.vtt (гэп в записи)
  → Ближайший спикер из zoom.vtt
```

## ✅ Гарантии:

1. **Текст из zoom.vtt НЕ используется НИГДЕ**
   - Только timestamps + speaker names

2. **Весь текст в выходе - из turboscribe.vtt**
   - 100% качественный текст

3. **Спикеры - только из zoom.vtt**
   - Точные имена, как в Zoom записи

4. **LLM НЕ придумывает спикеров**
   - Выбирает только из кандидатов, найденных по времени в zoom.vtt

## 🔍 Пример полного цикла:

```
zoom.vtt (строки 142-145):
00:03:09.040 --> 00:03:09.940
Dan Khasis: Да слушаю    ← текст игнорируется!

00:03:09.940 --> 00:03:15.880
Serhii Kasainov: Так от  ← текст игнорируется!

turboscribe.vtt (строка 8):
00:03:09.940 --> 00:03:55.000
Так, от меня маленький апдейт по снапшотам. Мы очень долго...

ВЫРАВНИВАНИЕ:
1. turboscribe [09.94-55.00] накладывается на:
   - Dan [09.04-09.94]: 0 секунд (0%)
   - Serhii [09.94-15.88]: 5.94 секунд (13%)
   - ... другие сегменты: 39.06 секунд (87%)

2. Serhii доминирует в начале (13%)
   → Но другие спикеры тоже есть
   → Нужен LLM!

3. LLM получает:
   - Текст: "Так, от меня маленький апдейт..."
   - Кандидаты: Serhii (13%), другие (87%)
   - Контекст: предыдущий Dan говорил "Да, я слушаю"

4. LLM выбирает: Serhii (фраза "от меня" = начинает говорить)

РЕЗУЛЬТАТ в JSONL:
{
  "start": 189.94,
  "end": 235.00,
  "speaker": "Serhii Kasainov",  ← из zoom.vtt
  "text": "Так, от меня...",      ← из turboscribe.vtt
  "confidence": 0.95
}
```

---

**Итого:** zoom.vtt используется ТОЛЬКО как справочник "кто когда говорил", а весь текст - из turboscribe.vtt!

